{
  "buildCommand": "cd web && yarn build",
  "outputDirectory": "web/build",
  "installCommand": "yarn install --cwd web && yarn install --cwd ws",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" }
  ]
}
